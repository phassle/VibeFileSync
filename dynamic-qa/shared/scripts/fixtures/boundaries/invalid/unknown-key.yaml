# Inherited rule (unknown-key fail-closed): an unrecognized key on a
# boundary declaration is rejected, even in an otherwise policy-compliant
# document.
schema: dynamic-qa-flow-v1
id: boundary-unknown-key-flow
revision: 1
title: "Inherited rule: unknown boundary key is rejected"
intent: "Prove an unknown key on a boundary declaration still fails closed via #145's fixtures."
criticality: high
state: draft
origin:
  tickets:
    - "https://github.com/phassle/VibeFileSync/issues/18"
test_level:
  selection: inferred
data_sets: []
boundaries:
  - id: vibesync-cli
    system: "vibesync CLI"
    treatment: real
    behavior: "Invoke the CLI's Update mode against a temporary folder pair."
    side_effects: "none"
    role: owned
    selector: ".btn-primary"
steps:
  - id: given-a
    kind: given
    intent: "i"
  - id: when-b
    kind: when
    intent: "i"
  - id: then-c
    kind: then
    intent: "i"
    outcomes:
      - id: outcome-a
        expect: "exact one."
