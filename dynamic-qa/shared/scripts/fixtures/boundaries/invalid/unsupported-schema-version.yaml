# Inherited rule: an unsupported schema version is rejected rather than
# silently accepted or coerced, even in an otherwise policy-compliant
# boundaries document.
schema: dynamic-qa-flow-v2
id: boundary-unsupported-schema-version-flow
revision: 1
title: "Inherited rule: unsupported schema version is rejected"
intent: "Prove an unsupported schema version still fails closed via #145's fixtures."
criticality: high
state: draft
origin:
  tickets:
    - "https://github.com/phassle/VibeFileSync/issues/18"
test_level:
  selection: inferred
data_sets: []
boundaries:
  - id: vibesync-cli
    system: "vibesync CLI"
    treatment: real
    behavior: "Invoke the CLI's Update mode against a temporary folder pair."
    side_effects: "none"
    role: owned
steps:
  - id: given-a
    kind: given
    intent: "i"
  - id: when-b
    kind: when
    intent: "i"
  - id: then-c
    kind: then
    intent: "i"
    outcomes:
      - id: outcome-a
        expect: "exact one."
