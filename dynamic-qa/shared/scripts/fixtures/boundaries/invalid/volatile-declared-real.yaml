# Violates rule: a volatile boundary (third party, payments, time,
# randomness, unverified behaviour) can never be declared real.
schema: dynamic-qa-flow-v1
id: volatile-declared-real-flow
revision: 1
title: "Boundary policy violation: volatile boundary declared real"
intent: "Prove volatile: true forbids treatment: real."
criticality: high
state: draft
origin:
  tickets:
    - "https://github.com/phassle/VibeFileSync/issues/18"
test_level:
  selection: inferred
data_sets: []
boundaries:
  - id: vibesync-cli
    system: "vibesync CLI"
    treatment: real
    behavior: "Invoke the CLI's Update mode against a temporary folder pair."
    side_effects: "none"
    role: owned
  - id: payment-provider
    system: "the third-party payment provider"
    treatment: real
    behavior: "Calls out to the live payment provider."
    side_effects: "Would charge a real card."
    volatile: true
steps:
  - id: given-a
    kind: given
    intent: "i"
  - id: when-b
    kind: when
    intent: "i"
  - id: then-c
    kind: then
    intent: "i"
    outcomes:
      - id: outcome-a
        expect: "exact one."
